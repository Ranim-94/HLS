# TODO List

1- Understand the role of co-simulation

2- Review all done exercices after finishing the course

3- To do the exercies on the 7 seg (in the pdf part)
    - Also the ones regarding BCD code and input binary number

4- About the data types such as for example `ap_uint<4> control_signals` (project `Seven_Seg_Single_Digit_Vitis`):

- In the code, for example I use `*control_signals = 0b1110;` 
- In the testbenc file , I did a comparison `control_signals != 14`
- To see later the conversion concept between binary, base 10 , how the format is used
  - But this is after reviewing number representation concept to refresh the concepts

5- To see this issue about the port `ap_rst` (it is show)
    - it is always shown as extra port